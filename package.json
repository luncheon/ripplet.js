{
  "name": "ripplet.js",
  "version": "0.0.4",
  "description": "Most lightweight material design ripple effect generator with no dependencies",
  "main": "./dist/index.min.js",
  "types": "./lib/index.d.ts",
  "module": "./es/index.js",
  "jsnext:main": "./es/index.js",
  "files": [
    "dist",
    "es",
    "lib",
    "src"
  ],
  "scripts": {
    "rebuild": "npm run clean && npm run build",
    "build": "npm run lint && npm run build:dist && npm run build:lib && npm run build:es",
    "build:dist": "rollup -c && uglifyjs -m -o dist/index.min.js dist/index.js",
    "build:lib": "tsc -p . -d",
    "build:es": "tsc -p . -t ES2015 --outDir ./es",
    "watch": "rollup -c -w",
    "clean": "rimraf dist lib es",
    "lint": "tslint -p . --type-check",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "ripplet",
    "ripplet.js",
    "ripple",
    "ripple.js",
    "ripplify",
    "waves",
    "material",
    "material design",
    "material component"
  ],
  "author": "",
  "license": "WTFPL",
  "repository": "luncheon/ripplet.js",
  "devDependencies": {
    "rimraf": "2.6.2",
    "rollup": "0.50.0",
    "tslint": "5.8.0",
    "typescript": "2.5.3",
    "uglify-es": "3.1.5",
    "uglify-js": "3.1.5",
    "umd": "3.0.1"
  }
}
